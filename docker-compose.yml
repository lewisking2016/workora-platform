version: '3.3'

services:
  backend:
    build: ./workora-backend
    ports:
      - "3001:3001"
    env_file:
      - ./workora-backend/.env
    environment:
      - PORT=3001
    restart: always

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    restart: always
